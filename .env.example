# Copy to .env (or export directly in the shell) and fill in the real value.
# NEVER commit the populated file - .env is git-ignored.
#
# Elasticsearch HTTP Basic credential, already base64 encoded:
#   printf '%s' 'username:password' | base64
#
ELASTIC_AUTH='Basic <base64 of "username:password">'
